# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~
GET        /api/$name$/hello        $organization;format="packaged"$.$name;format="word"$.HelloController.hello(req: Request)
